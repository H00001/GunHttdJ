#this is a config file
#you can write profile file in it
#
core {
  maxRunnningNum = 8
  port = 8822
  clientWaitTime = 50
  fileReadBufferMin = 512
  connection = 1
  profileName = banner.txt
   initWait = 1000
   minInterval = 1000
}
log {
  outputlevel = 0
  direct = console
}
+conf/http.conf